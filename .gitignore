# build output
dist/
.astro/

# deps
node_modules/

# env (keep the example)
.env
.env.*
!.env.example
# Non-secret PUBLIC_* analytics config — see the file header.
!.env.production

# data (future SQLite volume)
data/

# misc
*.log
coverage/
.DS_Store
