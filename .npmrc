# better-auth declares an optional peer (@tanstack/react-start) that pins a
# newer vite than Astro uses. We don't use that integration (this is an Astro
# app), so accept the resolution. Applies to `npm install` and `npm ci` (Docker).
legacy-peer-deps=true
